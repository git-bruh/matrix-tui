#include "unity.h"
#include "widgets.h"

#include <stdbool.h>
/* Stub */
void
node_draw_cb(void *data, struct widget_points *points, bool is_selected) {
	TEST_ASSERT_TRUE(false);

	(void) data;
	(void) points;
	(void) is_selected;
}
